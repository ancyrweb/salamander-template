import {User} from "../../orm/entity/User";
import {AsyncOperation} from "../../typings/types";

class UserManager {
  save(user: User) {

  }

  createTokenFromCredentials(email: string, password: string) {

  }
}

export default new UserManager();
